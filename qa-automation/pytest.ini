[pytest]
testpaths = api-tests database-tests
python_files = test_*.py
markers =
    smoke: fast, high-value checks safe to run on every push
    regression: full suite, run on a schedule or before release
addopts = -ra --strict-markers
